# Clean: Vestigial code. This should be removed at some point. (just leave it here in case my project breaks)

# from setuptools import setup, find_packages

# setup(
#     name='pysfo',
#     version='0.1',
#     packages=find_packages(where='src'),
#     package_dir={'': 'src'},
# )